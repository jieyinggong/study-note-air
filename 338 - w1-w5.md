
下面是 **Week 1 + Week 2** 的中文整理版。我严格按 PPT 的内容来讲，尽量做到**清晰、完整、不中断逻辑、不遗漏重点**。内容主要依据 Week 1 和 Week 2 讲义。  

---

# **Week 1：Approximations（近似）**

  

## **1. 为什么需要近似**

  

这门课一开始先回答一个最根本的问题：**为什么数值分析需要“近似”**。

  

现实中的很多问题都属于**连续数学**，比如：

- 实数
    
- 导数
    
- 非线性函数
    
- 面积、速度、曲率等连续量
    

  

但计算机本质上只能处理**离散的、有限长度表示**的数据，不能真正精确表示所有实数。

所以很多连续数学问题，**不能通过有限步算法得到真正的精确解**。这时我们就只能求一个**近似解**，并希望误差尽可能小。

---

## **2. 一个计算问题的例子：地球表面积**

  

PPT 用“计算地球表面积”说明一个实际计算问题是怎么出现误差的。

  

目标：计算地球表面积。

一个近似方案是：

  

第一步，把地球**建模**成球体。

这是合理的，因为：

1. 地球大致像球
    
2. 球的表面积公式我们知道
    

  

第二步，取半径

r \approx 6371 \text{ km}

  

然后计算：

4\pi r^2 \approx 4 \times 3.14 \times 6371^2 \approx 509805891 \approx 509.8 \text{ million km}^2

  

而真实值更接近 510.1 million km²。

这说明误差不是单一来源，而是多个误差叠加。

---

## **3. 四种误差来源**

  

这个例子里出现了四类误差：

  

### **3.1 Modelling Error（建模误差）**

  

因为地球并不是真正的球体。

也就是说，**数学模型本身就和真实世界有差距**。

  

### **3.2 Empirical Error（经验/测量误差）**

  

半径 r 不是绝对精确测出来的。

输入数据本身就可能不准。

  

### **3.3 Truncation Error（截断误差）**

  

例如把 \pi 用 3.14 代替。

本质上是：**原本一个无限过程或更高精度表达，被截断成有限项**。

  

### **3.4 Rounding Error（舍入误差）**

  

例如把 509805891 再写成 509800000。

这是因为计算机或人工表达时，用有限位数去近似一个数。

---

## **4. Rounding Error 和 Truncation Error 的区别**

  

这两个概念很容易混。

  

### **Rounding Error（舍入误差）**

  

来自：

- 实数本身无法被精确表示
    
- 算术运算结果也未必能被机器精确表示
    

  

也就是说，**是表示和运算层面的误差**。

  

### **Truncation Error（截断误差）**

  

来自：

- 一个本来可以通过迭代、级数、极限逐渐逼近的数学过程
    
- 但我们在中途停下了，只取有限步
    

  

例如：

e = \frac1{0!}+\frac1{1!}+\frac1{2!}+\frac1{3!}+\cdots

  

如果只算前 4 项，那和真正的 e 之间就有截断误差。

所以可以简单记：

- **舍入误差**：机器表示不精确
    
- **截断误差**：算法过程没做完
    

---

## **5. 什么时候要“数值地”解决问题**

  

PPT 强调：不是所有问题都直接上算法。

在数值求解之前，首先要考虑这个问题本身是否值得、是否适合数值处理。

---

## **6. 在解决一个问题前：Well-posedness（适定性）**

  

一个问题要先满足**well-posed**，也就是“适定”。

  

PPT 给出的要求是：

1. **解存在**
    
2. **解唯一**
    
3. **解对输入数据连续依赖**
    

  

前两条比较直接。第三条的意思是：输入变一点，解不应该突然跳得特别厉害。

不过 PPT 也提醒：教材有时会把这条说得更像“well-conditioning”的意思。严格说：

- **well-posedness** 更偏“问题定义层面有没有正常解”
    
- **well-conditioning** 更偏“解对输入扰动敏不敏感”
    

---

## **7. 解决一个计算问题的三步**

  

PPT 总结了数值计算的一般流程：

1. **建立数学模型**
    
2. **设计数值算法**
    
3. **实现算法并运行，得到结果**
    

  

前提是：问题最好同时满足

- well-posed
    
- well-conditioned
    

---

## **8. 误差的分类：Absolute / Relative / Forward / Backward**

  

这是 Week 1 的核心之一。

  

### **8.1 Absolute Error（绝对误差）**

  

\text{Absolute Error} = \text{Approximate value} - \text{True value}

  

就是“近似值减真值”。

  

### **8.2 Relative Error（相对误差）**

  

\text{Relative Error}=\frac{\text{Absolute Error}}{\text{True Value}}

  

它反映误差相对真值有多大。

但 PPT 也提醒，这个定义有时会有问题，比如真值很接近 0 时，相对误差就可能不太好用。

---

## **9. 总误差如何拆开**

  

PPT 把总误差写成：

  

\tilde f(\hat x)-f(x)

  

其中：

- f(x)：真正想要的精确值
    
- f_{\text{alg}}(x)：你设计的数学近似模型
    
- \tilde f(\hat x)：计算机最终真正算出来的结果
    

  

于是：

  

\tilde f(\hat x)-f(x) = \big(\tilde f(\hat x)-f_{\text{alg}}(\hat x)\big) + \big(f_{\text{alg}}(\hat x)-f(\hat x)\big) + \big(f(\hat x)-f(x)\big)

  

分别对应：

1. **Rounding Error**
    
2. **Truncation Error**
    
3. **Propagated Data Error**（输入误差传播）
    

---

## **10. 这三个对象各自代表什么**

  

PPT 专门澄清了三个符号：

  

### f(x)

  

理想中的精确目标值。

也就是“真正想算的数学对象”。

  

### f_{\text{alg}}(x)

  

你设计出来的数学近似。

截断误差来自这里。

  

### \tilde f(\hat x)

  

机器最后算出来的值。

舍入误差来自这里，因为计算机每一步四则运算都可能引入误差。

---

## **11. 例子：近似计算** \cos(\pi/6)

  

PPT 用这个例子展示误差分层。

  

目标：

f(x)=\cos x,\quad x=\frac{\pi}{6}

  

先用 Maclaurin 级数的二次多项式近似：

f_{\text{alg}}(x)=1-\frac{x^2}{2}

  

再把

\frac{\pi}{6}\approx 0.523

取成 \hat x=0.52

  

于是：

f_{\text{alg}}(0.52)=1-\frac{0.52^2}{2} =1-\frac{0.2704}{2} =1-0.1352 =0.8648

  

如果机器只能保留 3 位小数，那么最后算出来可能是：

\tilde f(\hat x)=1-\frac{0.270}{2}=0.865

  

这时绝对误差大约是：

0.865-\cos(\pi/6)\approx 0.001

  

这里忽略高次项带来的就是**截断误差**。

---

## **12. Forward Error 和 Backward Error**

  

这是数值分析特别重要的一组概念。

  

假设我们要计算

y=f(x)

但最后机器给出的是 \hat y。

  

### **Forward Error（前向误差）**

  

就是直接看输出偏了多少。

也就是“算出来的结果”和“真实结果”之间的误差。

---

### **Backward Error（后向误差）**

  

后向误差换了个角度思考：

  

不是问“输出差多少”，

而是问：

  

**这个算出来的** \hat y**，是否等于某个离原输入** x **很近的输入** \tilde x **的精确输出？**

  

即

f(\tilde x)=\hat y

  

那么：

\Delta x=\tilde x-x

就是 backward error。

---

## **13. Backward Error 的意义**

  

如果 backward error 很小，就说明：

  

> 虽然我们没精确算出 f(x)，但我们精确算出了一个“附近问题”的解。

  

这通常被认为是一个好的数值性质。

因为真实数据本身往往也带有不确定性，所以如果你求出的其实是“一个非常接近原问题”的精确解，已经很不错了。

---

## **14. Backward Error 例子**

  

继续用

f(x)=\cos x

  

若取 x=1，则

f(1)\approx 0.5403

  

而近似公式

\hat y=1-\frac{x^2}{2}

给出

\hat y=0.5

  

现在问：哪个输入 \tilde x 会满足

\cos(\tilde x)=0.5

得到：

\tilde x=\cos^{-1}(0.5)\approx 1.0472

  

所以 backward error 为：

\Delta x=1.0472-1=0.0472

  

这个值不大，所以从 backward error 的角度看，这个近似还可以。

---

## **15. Sensitivity 和 Conditioning（敏感性与条件性）**

  

PPT 这里讨论：**输入数据的小变化，会怎样影响输出？**

  

如果输入发生一个小比例变化，输出也只发生类似规模的比例变化，那么这个问题就是**well-conditioned（良态）**。

否则就是 **ill-conditioned（病态）**。

---

## **16. Condition Number（条件数）**

  

PPT 给出：

CN=\frac{|\Delta y/y|}{|\Delta x/x|}

  

也就是：

\text{相对前向误差} = CN \times \text{相对后向误差}

  

所以：

- CN 小：问题不敏感
    
- CN 大：问题很敏感
    

  

当 \Delta x 很小时，还可以近似推出：

CN \approx \frac{x f'(x)}{f(x)}

  

这说明条件数和导数有关，本质是在量化“输入被放大到输出的程度”。

---

## **17. Stability 和 Accuracy（稳定性与准确性）**

  

### **Stability（稳定性）**

  

一个算法是稳定的，如果它产生的结果可以看成是**某个附近问题的精确解**。

也就是：**backward error 小**。

  

### **Accuracy（准确性）**

  

准确性说的是：最终结果到底离真解近不近。

  

### **二者关系**

  

PPT 特别强调：

- **稳定性本身不保证准确**
    
- 但是
    
    **稳定算法 + well-conditioned 问题 ⇒ 准确结果**
    

  

这是数值分析里非常核心的一句。

---

## **18. Accuracy vs Precision**

  

PPT 用数

3.14285714

说明：

- 它很**precise**：位数很多
    
- 但不一定很**accurate**：未必更接近 \pi
    

  

所以：

- **precision**：位数多、表示细
    
- **accuracy**：离真值近
    

---

## **19. Week 1 最后：引入 Computer Arithmetic**

  

Week 1 最后开始引出下周内容：计算机算术。

核心思想是：

- 计算机不能精确表示所有实数
    
- 只能用有限位数存储近似值
    
- 所以下周开始系统讲 floating-point（浮点数系统）
    

---

# **Week 2：Computer Arithmetic（计算机算术）**

---

## **1. Week 2 的核心主题**

  

这一周主要讲：

- 有限精度算术
    
- 浮点数系统
    
- 规格化表示
    
- 舍入与机器精度
    
- 上溢/下溢
    
- 渐进下溢
    
- 浮点运算性质
    
- 灾难性消去
    

---

## **2. Finite-Precision Arithmetic（有限精度算术）**

  

现实中的实数很多都无法被计算机精确表示，比如：

- \pi
    
- 很长的小数
    
- 无限不循环小数
    

  

所以在计算机里，一个实数只能被表示成**有限长度的字符串**，本质上是一个**近似值**。

这就是有限精度算术的起点。

---

## **3. Floating-Point Number（浮点数）**

  

我们平时会把

0.000000007396

写成

7.396 \times 10^{-9}

  

这就是科学计数法。

浮点数系统就是把这种表示方式系统化、一般化。

  

PPT 给出的浮点数一般形式是：

  

x=\pm(d_0+d_1\beta^{-1}+d_2\beta^{-2}+\cdots+d_{p-1}\beta^{-(p-1)})\beta^E

  

其中：

- \beta：底数（base/radix）
    
- p：精度（precision）
    
- L,U：指数范围
    
- d_i：每一位数字，满足 0\le d_i\le \beta-1
    
- E：指数，满足 L\le E\le U
    

  

这整个集合记作某个浮点数系统 \mathbb F。

---

## **4. 浮点数系统由哪四个参数决定**

  

一个浮点数系统由 4 个整数决定：

  

\beta,\ p,\ L,\ U

  

分别表示：

- \beta：底数
    
- p：精度
    
- L：最小指数
    
- U：最大指数
    

  

另外：

- d_0d_1\cdots d_{p-1} 叫 **mantissa / significand（尾数/有效数）**
    
- d_1\cdots d_{p-1} 叫 fraction
    
- E 叫 exponent（指数）
    

---

## **5. IEEE 单精度例子**

  

PPT 举了 IEEE 单精度（SP）系统：

  

\beta=2,\quad p=24,\quad L=-126,\quad U=127

  

并举例把十进制数 0.15625 转成二进制：

  

0.15625=\frac18+\frac1{32}=0.00101_2

  

也可以写成规格化形式：

1.01\times 2^{-3}

  

在 IEEE 浮点表示里：

- 前导的 1 在规格化二进制中总是存在，所以通常**不存**
    
- 指数用**偏置（bias）**方式存储
    

---

## **6. 指数是怎么存的：Bias（偏置）**

  

指数位不是直接存 E，而是存“偏置后的指数”。

  

对于单精度：

- bias = 127
    
- 指数 0 存成 127
    
- 指数 -3 存成 127-3=124
    

  

而 124 的二进制是：

01111100

  

所以浮点数中的指数部分实际上是“加了偏移量后的整数编码”。

---

## **7. 为什么要用浮点数系统**

  

PPT 给了三个原因：

  

### **7.1 计算机字长有限**

  

机器只能存有限位。

  

### **7.2 能表示数量级差异很大的数**

  

浮点数通过指数部分，使得非常大或非常小的数都能表示。

  

### **7.3 不浪费位数在很多前导 0 上**

  

比如一个很小的数，如果不用浮点方式，就会浪费很多位存 0。

浮点表示能把位数集中在“有效数字”上，提升有效精度。

---

## **8. Normalized Floating-Point System（规格化浮点系统）**

  

规格化要求：

  

> 只有当这个数本身为 0 时，首位 d_0 才能为 0。

  

也就是说，非零数必须以非零首位开头。

  

### **为什么规格化有用？**

  

#### **8.1 保证表示唯一**

同一个数不会有很多种等价写法。

  

#### **8.2 在二进制里能多省一位**

因为非零数首位一定是 1，所以这个 1 可以不存。

这样实际上能多出一位精度。

---

## **9. 浮点系统的缺陷**

  

PPT 提到四类主要问题：

1. **Rounding（舍入）**
    
2. **Overflow（上溢）**
    
3. **Underflow（下溢）**
    
4. **Catastrophic cancellation（灾难性消去）**
    

---

## **10. 为什么会需要 rounding（舍入）**

  

假设两个精度都是 p 的浮点数相乘。

它们的乘积可能需要接近 2p 位精度才能精确表示。

  

例如：

x=5.9\times 10^2,\quad y=6.1\times 10^1

则

xy=3.599\times 10^4

  

如果系统精度不够，就必须把结果再近似回当前浮点系统中。

加法和除法也可能遇到同样问题。

---

## **11. 两种舍入方式**

  

### **11.1 Round to nearest（四舍五入到最近）**

  

例如：

5.10072 \to 5.101

  

PPT 还提到 binary 下常见的 round to even。

  

### **11.2 Chop（截断，朝 0 舍入）**

  

例如：

5.10072 \to 5.100

  

也就是直接砍掉后面的位。

---

## **12. 机器精度：**\epsilon_{\text{mach}}

  

Week 2 的重点之一。

  

### **定义 1**

  

\epsilon_{\text{mach}} 是把一个非零实数 x 用机器表示成 fl(x) 时，可能产生的**最大相对误差**。

  

即：

\left|\frac{fl(x)-x}{x}\right|\le \epsilon_{\text{mach}}

  

它刻画的是这个浮点系统的精度上限。

---

## **13. 机器精度的第二种定义**

  

### **定义 2**

  

\epsilon_{\text{mach}} 也可以定义成：

  

> 比 1 大的最小可表示浮点数 x 与 1 的差

  

也就是最小的 \epsilon，使得：

fl(1+\epsilon)>1

  

基于这个定义，PPT 给出：

- 如果用 chopping：
    
    \epsilon_{\text{mach}}=\beta^{-p+1}
    
- 如果用 round to nearest：
    
    \epsilon_{\text{mach}}=0.5\beta^{-p+1}
    

  

后者更小，说明四舍五入通常更精确。

---

## **14. 两个定义的关系**

  

PPT 说这两个定义**不完全等价**，但定义 2 给出了定义 1 的一个上界理解。

而且最坏的相对误差通常发生在“从右侧靠近 1 的那些数被舍入到 1”时。

因此 machine epsilon 也叫 **unit roundoff**。

---

## **15. 可表示数在实数轴上的分布特点**

  

PPT 强调一个很重要的现象：

  

> 可表示浮点数并不是均匀分布的。

  

越靠近 0，数之间的间距越密；

数值越大，相邻可表示数之间的间隔越大。

  

所以浮点数系统不是“固定绝对精度”，而是更像“固定相对精度”。

---

## **16. Underflow Level（下溢阈值）**

  

最小的正的**规格化**浮点数称为 underflow level：

  

UFL=\beta^L

  

如果某个非零计算结果比它还小，就可能无法以规格化形式表示。

---

## **17. Overflow Level（上溢阈值）**

  

最大的浮点数为：

  

OFL=(1-\beta^{-p})\beta^{U+1}

  

如果两个很大的正数相乘，真实结果超过 OFL，那么机器就会发生 overflow。

  

PPT 还问：overflow 和 underflow 哪个更严重？

通常 overflow 更危险，因为数值直接爆掉；underflow 有时只是太接近 0。

---

## **18. Gradual Underflow（渐进下溢）**

  

在规格化系统中，0 附近有个空隙：(-UFL, UFL)。

  

为了填补这段空隙，可以允许在 E=L 时首位 d_0=0。

这样就得到 **denormalized / subnormal numbers（非规格化数/次正规数）**。

  

### **好处**

  

能表示更多靠近 0 的小数。

  

### **代价**

  

这些数的有效精度更低，而且 machine epsilon 并不会因此变小。

---

## **19. 浮点运算不满足普通实数运算律**

  

因为每一步都要舍入，所以我们熟悉的实数代数规律在浮点运算中不一定成立。

  

PPT 举的例子是：

加法不一定满足结合律。

  

如果某个 \epsilon<\epsilon_{\text{mach}}，但 2\epsilon>\epsilon_{\text{mach}}，那么：

- (1+\epsilon)+\epsilon 和
    
- 1+(\epsilon+\epsilon)
    

  

可能得到不同结果。

  

这说明浮点数里运算顺序真的会影响结果。

---

## **20. 调和级数在浮点数里会“收敛”**

  

PPT 提到：

  

\sum_{i=1}^{\infty}\frac1i

在数学上发散，但在浮点运算中会收敛。

  

原因是：

后面越来越小的项加到当前和上时，可能小到已经低于机器分辨能力，被直接舍掉，结果不再变化。

---

## **21. Catastrophic Cancellation（灾难性消去）**

  

这是 Week 2 另一个非常重要的概念。

  

当两个**很大且很接近**的数相减时，前面很多有效数字会互相抵消，结果只剩下少量有效位。

这会导致**严重的信息损失**。

  

例如若某个 \epsilon<\epsilon_{\text{mach}}，但 2\epsilon>\epsilon_{\text{mach}}，则：

  

(1+\epsilon)-1

  

在机器里可能直接变成 0，虽然真实结果并不为 0。

这就是灾难性消去。

---

## **22. Week 2 最后的课程展望**

  

PPT 最后说，后面会把这些浮点、误差、条件数等思想应用到：

- 线性方程组
    
- 非线性方程
    
- 非线性优化
    
- 线性最小二乘
    

---

# **Week 1–2 总结版（考试复习重点）**

  

## **Week 1 你必须抓住的主线**

  

Week 1 的核心是：

  

### **1. 为什么要近似**

  

因为连续数学问题通常无法被计算机精确求解，只能近似。

  

### **2. 误差来源**

- modelling
    
- empirical
    
- truncation
    
- rounding
    

  

### **3. 误差分析框架**

- absolute error
    
- relative error
    
- forward error
    
- backward error
    

  

### **4. 问题和算法要分开看**

- 问题：well-posed / well-conditioned
    
- 算法：stable / accurate
    

  

### **5. 最重要的关系**

  

**stable algorithm + well-conditioned problem ⇒ accurate solution** 

---

## **Week 2 你必须抓住的主线**

  

Week 2 的核心是：

  

### **1. 实数在机器里只能有限表示**

  

所以一定会有舍入和表示误差。

  

### **2. 浮点数的四个参数**

  

\beta,\ p,\ L,\ U

一定要知道各自代表什么。

  

### **3. 规格化表示**

- 唯一表示
    
- 二进制能省掉 leading 1
    

  

### **4. machine epsilon**

  

它衡量机器精度，是 floating-point 误差分析里的核心量。

  

### **5. underflow / overflow / gradual underflow**

  

要知道含义和区别。

  

### **6. 浮点数不满足普通实数运算律**

  

尤其是结合律可能失效。

  

### **7. catastrophic cancellation**

  

两个接近的大数相减会丢失有效数字，这是考试里很爱考的概念。

---

如果你愿意，我下一条可以继续给你整理成一份 **Week 1–2 中文精简背诵版 / cheat sheet 版**。

下面是 **Week 3 + Week 4** 的中文整理版。

我会按 PPT 的逻辑顺序来讲，做到**清晰、完整、不遗漏**；同时你要求的 **专业/学术名词我都会保留英文标注**。内容依据 Week 3 与 Week 4 讲义。  

---

# **Week 3：Linear Systems on** \mathbb R

---

## **1. Linear Relationship（线性关系）**

  

最简单的线性关系是比例关系：

  

f(x)=ax,\quad f:\mathbb R\to\mathbb R

  

因为输出和输入之间是直线关系，所以叫 **linear**。

  

如果输入不止一个，就可以写成：

  

f(x_1,\dots,x_n)=a_1x_1+\cdots+a_nx_n

  

这时 f:\mathbb R^n\to\mathbb R。

也就是说，**多个输入的线性组合** 仍然是线性关系。

---

## **2. Involving more outputs（多个输出）**

  

如果函数从 \mathbb R^n 映到 \mathbb R^m，也就是：

  

f:\mathbb R^n\to\mathbb R^m

  

那么可以理解成：**有** m **个线性关系同时发生**。

  

也就是：

  

f(x_1,\dots,x_n)=\big(f_1(x_1,\dots,x_n),\dots,f_m(x_1,\dots,x_n)\big)

  

其中每个 f_i 都是一个从 \mathbb R^n\to\mathbb R 的线性函数：

  

f_i(x_1,\dots,x_n)=a_{i,1}x_1+\cdots+a_{i,n}x_n

  

所以，多输出线性关系本质上就是很多个单输出线性关系拼在一起。

---

## **3. Matrix Representation（矩阵表示）**

  

这种线性关系可以统一写成 **matrix multiplication（矩阵乘法）**：

  

f(x_1,\dots,x_n)=A \begin{bmatrix} x_1\\ \vdots\\ x_n \end{bmatrix}

  

其中 A 是一个 m\times n 的 **coefficient matrix（系数矩阵）**。

这说明：

  

> 线性函数可以用矩阵来表达。

  

这也是后面整章都在研究的核心对象。

---

## **4. Linearization（线性化）**

  

PPT 还提醒：很多 **differentiable functions（可微函数）**，在局部都可以被 **linear function（线性函数）** 近似。

这叫：

- **linear approximation**
    
- 或 **tangent line approximation**
    

  

也就是说，即使原函数不是线性的，我们也常常先在局部把它看成线性的，这也是线性系统如此重要的原因之一。

---

## **5. Systems of Linear Equations（线性方程组）**

  

线性方程组来自很多应用。我们想找出某些 x_1,\dots,x_n，使得一组方程同时成立：

  

a_{i,1}x_1+\cdots+a_{i,n}x_n=b_i,\quad i=1,\dots,m

  

也就是 m **个方程，**n **个未知数**。

---

## **6. The problem（问题本身）**

  

Week 3 的核心问题是：

  

A\mathbf x=\mathbf b

  

其中：

- A：给定的 m\times n 矩阵
    
- \mathbf b\in\mathbb R^m
    
- 求 \mathbf x\in\mathbb R^n
    

  

这就是 **solving a linear system（求解线性方程组）**。

然后课程马上问两个关键问题：

1. 什么时候这个问题是 **well-posed（适定）**？
    
2. 什么时候这个问题是 **well-conditioned（良条件）**？
    

---

## **7. Well-posed（适定）**

  

回忆 Week 1，**well-posed** 的要求是：

- **solution exists（解存在）**
    
- **solution is unique（解唯一）**
    
- **solution depends continuously on the data（解连续依赖输入）**
    

  

在线性系统这里，重点先放在前两条：存在与唯一。

---

## **8. Solution existence（解存在）**

  

解存在，意味着系统是 **consistent（一致的）**。

  

直观上说，一致就是各个方程之间**不互相冲突**。

在线性代数里，一个系统一致当且仅当：

  

\operatorname{rank}(A)=\operatorname{rank}([A|b])

  

其中 [A|b] 是 **augmented matrix（增广矩阵）**。

---

## **9. Augmented Matrix（增广矩阵）和 Rank（秩）**

  

增广矩阵写成：

  

[A|b]

  

也就是把右端向量 b 接到矩阵 A 的最后一列。

  

**rank（秩）** 有很多等价定义，PPT提醒要都复习。

直观上可以理解为：

  

> 描述这个方程组解空间所需的独立方程数量。

  

也可以理解成 row reduction 后 **nonzero pivots（非零主元）** 的个数。

---

## **10. Solution uniqueness（解唯一）**

  

即使系统 consistent，也不一定只有一个解。

尤其当 m<n 时，也就是方程个数少于未知数个数，这种系统通常是 **underdetermined（欠定）** 的，往往会有多个解。

---

## **11. Consistency 和 Uniqueness 的总体判定**

  

PPT 总结得很清楚，关键看：

  

\operatorname{rank}(A)\ \text{和}\ \operatorname{rank}([A|b])

  

### **情况 1：**

  

如果

\operatorname{rank}(A)<\operatorname{rank}([A|b])

那么 **no solution（无解）**。

  

### **情况 2：**

  

如果

\operatorname{rank}(A)=\operatorname{rank}([A|b])

那么系统有解。接着分两种：

- 若 rank = n，则 **unique solution（唯一解）**
    
- 若 rank < n，则 **infinitely many solutions（无穷多解）**
    

  

### **情况 3：**

  

\operatorname{rank}(A)>\operatorname{rank}([A|b])

这是不可能的。

---

## **12. 所以什么时候问题是 Well-posed**

  

一个线性方程组问题是 well-posed，当且仅当：

  

\operatorname{rank}(A)=\operatorname{rank}([A|b])=n

  

也就是：

- 有解
    
- 且唯一
    

  

PPT 接着指出，这种情况只有在 n\le m 时才可能。

再往下课程把重点缩小到最重要的一类：

  

> **square systems（方阵系统）**，即 n 个方程、n 个未知数。

---

## **13. Focus：square nonsingular matrix（可逆方阵）**

  

之后课程集中研究：

- A 是 **square matrix（方阵）**
    
- A 是 **nonsingular / invertible（非奇异 / 可逆）**
    

  

因为：

  

A \text{ invertible } \iff \text{ system has a unique solution}

  

所以，对 square system 来说：

  

> 问题 well-posed 当且仅当 A 是 nonsingular。

---

## **14. Well-conditioned（良条件）**

  

即使问题 well-posed，也不代表它数值上好算。

还要看它是不是 **well-conditioned**。

  

含义是：

  

> 输入数据有一个小的 **relative perturbation（相对扰动）**，输出解也只产生同等级别的相对变化。

  

否则就是 **ill-conditioned（病条件）**。

---

## **15. Input data（输入数据）是什么**

  

在线性系统 A\mathbf x=\mathbf b 中，输入数据包括：

- 矩阵 A 里的所有 a_{ij}
    
- 向量 b 的所有分量
    

  

这些输入都可能分别被扰动。

但它们对解的影响是整体性的，所以我们需要一个办法来度量“整体扰动”，这就引出 **norm（范数）**。

---

## **16. Vector Norms（向量范数）**

  

**vector norm** 是绝对值和距离概念在向量上的推广。

  

p-norm 定义为：

  

\|x\|_p=\left(\sum_{i=1}^n |x_i|^p\right)^{1/p}

  

常见的有：

- **1-norm**
    
- **2-norm**
    
- \infty**-norm**
    

  

其中：

  

\|x\|_\infty=\max_{1\le i\le n}|x_i|

  

是无穷范数。

---

## **17. 向量范数的重要性质**

  

任何 norm 都满足：

  

### **1. Positive definiteness（正定性）**

  

\|x\|=0 \iff x=0

  

### **2. Homogeneity（齐次性）**

  

\|\gamma x\|=|\gamma|\|x\|

  

### **3. Triangle inequality（三角不等式）**

  

\|x+y\|\le \|x\|+\|y\|

  

这些性质后面推导 condition number 时会用到。

---

## **18. Matrix Norms（矩阵范数）**

  

矩阵范数定义为：

  

\|M\|_q=\max_{x\ne 0}\frac{\|Mx\|_q}{\|x\|_q}

  

也可以等价写成：

  

\|M\|_q=\max_{\|x\|_q=1}\|Mx\|_q

  

这表示：矩阵 M 最多能把一个向量放大多少倍。

---

## **19. Conditioning（条件性）怎么定义到线性系统上**

  

对方程组来说，解可以写成：

  

x=A^{-1}b

  

所以可以把“求解线性系统”看成是函数：

  

f(A,b)=A^{-1}b

  

或者更简单地，把 b 看成输入，A^{-1} 看成作用在 b 上的函数。

  

于是 condition number 的思想就是：

  

\frac{\text{relative change in solution}}{\text{relative change in input}}

---

## **20. Matrix norm 的一个关键性质：Submultiplicative（次乘性）**

  

PPT 强调矩阵范数一个很重要的性质：

  

\|Mx\|_p \le \|M\|_p\|x\|_p

  

这个性质使得我们可以估计线性系统的敏感度。

---

## **21. Condition Number of a matrix（矩阵条件数）**

  

对一个 nonsingular matrix A，定义：

  

\operatorname{Cond}(A)=\|A^{-1}\|_p\|A\|_p

  

这就是 **condition number（条件数）**。

  

如果这个值小，问题是 **well-conditioned**；

如果这个值很大，问题是 **ill-conditioned**。

  

有些教材还规定：如果 A 是 singular，则 \operatorname{Cond}(A)=\infty。

---

## **22. 几何理解：well-conditioned vs ill-conditioned**

  

PPT 提到二维线性系统的几何图像：

- 两条直线交角比较大时，解对扰动不太敏感，系统更 **well-conditioned**
    
- 两条直线几乎平行时，交点对小扰动非常敏感，系统更 **ill-conditioned**
    

  

也就是说，**几何上“接近退化”的系统，数值上往往也很敏感**。

---

## **23. 现在还缺什么：Stable algorithm（稳定算法）**

  

有了 well-conditioned problem 还不够，

还需要一个 **stable algorithm（稳定算法）** 来真正算解。

  

PPT 强调：

  

> 我们的目标不是去算 A^{-1} 本身，除非题目真的要求逆矩阵。

> 一般来说，直接算 inverse 是不好的做法，尤其有 rounding errors 时。

  

所以更好的目标是：**直接求解** Ax=b。

---

## **24. Row reduction（消元）与 Gaussian elimination（高斯消元）**

  

把增广矩阵 [A|b] 做 **row reduction（行消元）**，把 A 变成 **upper triangular matrix（上三角矩阵）** U。

这样系统就变成：

  

Ux=b'

  

然后可以用 **backward substitution（回代）** 求解。

  

### **backward substitution 公式**

  

x_i=\frac{b'_i-\sum_{j=i+1}^n u_{ij}x_j}{u_{ii}}

  

因为上三角系统从最后一行开始很容易逐步解回去。

---

## **25. Elementary elimination matrices（初等消元矩阵）与 Permutation matrices（置换矩阵）**

  

PPT 解释：Gaussian elimination 可以看成一系列矩阵乘法。

- **elementary elimination matrix**：执行某一步消元
    
- **permutation matrix**：交换两行
    

  

其中 permutation matrix 的作用是做 **pivoting（选主元/换行）**。

---

## **26. LU Factorization（LU 分解）**

  

Gaussian elimination 把 A 变成 U。

如果把所有消元步骤组织起来，就能得到：

  

LU=PA

  

其中：

- L：**lower triangular matrix（下三角矩阵）**
    
- U：**upper triangular matrix（上三角矩阵）**
    
- P：**permutation matrix（置换矩阵）**
    

  

这就是 **LU factorization / LU decomposition（LU 分解）**。

---

## **27. 为什么 LU factorization 有用**

  

如果只解一次 Ax=b，LU 看起来和 Gaussian elimination 差不多。

但如果你要解很多个右端不同的系统：

  

Ax=b_1,\ Ax=b_2,\ \dots,\ Ax=b_r

  

而 A 不变，那么 LU 非常有用。

  

因为先做一次分解，然后每次只需要：

1. 解
    
    Ly=b
    
    用 **forward substitution（前代）**
    
2. 再解
    
    Ux=y
    
    用 backward substitution
    

---

## **28. Complexity Analysis（复杂度分析）**

  

如果每次都重新 Gaussian elimination，总代价是：

  

O(rn^3)

  

但如果先做一次 LU factorization：

- 分解一次：O(n^3)
    
- 每个新的 b_i 解一次：O(n^2)
    

  

总代价：

  

O(n^3+rn^2)

  

所以当右端很多时，LU 会大幅省计算量。

---

## **29. Pivots and small pivots（主元与小主元）**

  

PPT 最后强调：Gaussian elimination 的稳定性和 **pivot（主元）** 的选择密切相关。

  

如果主元太小，那么在消元时要除以很小的数，会把 rounding error 放大。

所以行的顺序很重要。

---

## **30. Pivoting（选主元）**

  

为了让 Gaussian elimination 更稳定，我们会做 **pivoting**：

- 避免除以 0
    
- 避免除以很小的数
    

  

PPT 说：

- **complete pivoting（完全选主元）**：更贵
    
- **partial pivoting（部分选主元）**：通常已经够好
    

---

# **Week 4：More on Linear Systems**

  

Week 4 是在线性系统基础上继续扩展，加入更实用、更数值分析化的内容。

---

## **1. Week 4 Topics（本周主题）**

  

PPT 列出的内容有：

- **Complete Pivoting**
    
- **Matrix Scaling for Conditioning**
    
- **Gauss-Jordan Elimination**
    
- **Rank-One Modifications**
    
- **Sherman-Morrison Formula**
    
- **Iterative Refinement**
    
- **Cholesky Factorization**
    
- **Linear Least Squares (LLS)**
    
- **Normal Equations** 
    

---

## **2. 回顾上周**

  

Week 4 一开始回顾 Week 3：

- 解线性系统 Ax=b
    
- 若 A 是 nonsingular square matrix，则问题 well-posed
    
- 若 \operatorname{cond}(A) 小，则问题 well-conditioned
    
- Gaussian elimination 是实践中稳定的算法之一
    
- 如果要多次求解相同 A 不同 b 的系统，LU factorization 很有用
    

---

## **3. Recall: Permutation matrices（置换矩阵回顾）**

  

**permutation matrix** 是把 identity matrix 的若干行交换得到的矩阵。

  

它有几个重要性质：

- 左乘 PA：交换 A 的行
    
- 右乘 AP：交换 A 的列
    
- permutation matrices 一般 **do not commute（不可交换）**
    
- 它的 inverse 等于 transpose：
    
    P^{-1}=P^T
    

  

这个性质后面在 pivoting 和 LU 中很常用。

---

## **4. Recall: Elementary Elimination Matrices（初等消元矩阵回顾）**

  

Week 4 重新提到 **elementary elimination matrices**。

它们对应的是“用一行去消掉下面若干行元素”的操作。

  

本质上：

- Gaussian elimination = 一串 elimination matrices 与 permutation matrices 的乘积作用在 A 上
    
- 这也是 LU factorization 的结构来源
    

---

## **5. Partial Pivoting（部分选主元）**

  

### **为什么要 pivoting**

  

如果某一步选到的 pivot 很小，那么消元时会放大 rounding errors。

所以要在当前列里找一个更大的元素来当 pivot。

  

### **Partial pivoting 的做法**

  

在当前列中，只在 pivot 下方的那些行里找**绝对值最大的元素**，把对应行换上来。

  

因此，在 partial pivoting 中，每个 elimination matrix M_k 前面会先有一个 permutation matrix P_k。

---

## **6. LU Factorization with pivoting（带 pivoting 的 LU 分解）**

  

Gaussian elimination 的目标仍然是把 A 化成 upper triangular matrix U。

  

在 partial pivoting 下：

  

U=MA

  

其中 M 是一串 permutation 和 elimination 矩阵的乘积。

重新整理后，可以写成：

  

LU=PA

  

其中 L 是 lower triangular。

也就是说，带 partial pivoting 的 LU factorization，本质上是：

  

> 先对 A 做行置换，再分解成 L 和 U。

---

## **7. Complete Pivoting（完全选主元）**

  

**complete pivoting** 比 partial pivoting 更进一步：

- partial pivoting：只在当前列往下找最大元素
    
- complete pivoting：在当前剩余的整个子矩阵里找最大元素
    

  

所以 complete pivoting 可能不仅交换行，还交换列。

于是分解形式变成：

  

LU=PAQ

  

其中：

- P：row permutation
    
- Q：column permutation
    

  

### **优缺点**

- **numerical stability（数值稳定性）** 更好
    
- 但 **computational cost（计算代价）** 更高
    

  

所以通常 partial pivoting 更常用。

---

## **8. Some matrices do not need pivoting（有些矩阵不需要选主元）**

  

PPT 说，有两类矩阵天然比较稳定，不太需要 pivoting：

  

### **8.1 Diagonally dominant matrix（对角占优矩阵）**

  

满足每一列或每一行的对角元素足够大，超过其他元素的绝对值和。

  

### **8.2 Symmetric positive definite matrix, SPD（对称正定矩阵）**

  

满足：

A=A^T

并且对所有非零向量 x：

x^TAx>0

  

这类矩阵尤其重要，因为它们可以用 **Cholesky decomposition（Cholesky 分解）**，而且不需要 pivoting。

---

## **9. Scaling（缩放）**

  

除了 pivoting，还有什么能改善 conditioning？

PPT 给出的答案是 **scaling（缩放）**。

  

缩放指的是：把某些行乘以常数。

这不会改变线性系统的解，但可能让数值计算表现更好。

  

常见写法是用一个 **diagonal matrix（对角矩阵）** 从左边乘：

  

DAx=Db

  

这样可以同时缩放多行。

---

## **10. Scaling Example（缩放例子）**

  

原系统：

  

\begin{bmatrix} 1 & 1\\ 0.00003 & 0.00004 \end{bmatrix} \begin{bmatrix} x_1\\ x_2 \end{bmatrix} = \begin{bmatrix} 2\\ 0.00007 \end{bmatrix}

  

第二行数值特别小，可能带来不好的数值性质。

  

乘上：

  

D= \begin{bmatrix} 1 & 0\\ 0 & 10^5 \end{bmatrix}

  

后变成：

  

\begin{bmatrix} 1 & 1\\ 3 & 4 \end{bmatrix} \begin{bmatrix} x_1\\ x_2 \end{bmatrix} = \begin{bmatrix} 2\\ 7 \end{bmatrix}

  

这个系统形式上更平衡，往往更好算。

这体现了 scaling 对 conditioning 的改善作用。

---

## **11. Gauss-Jordan Elimination（Gauss-Jordan 消元）**

  

普通 Gaussian elimination 把矩阵化成 upper triangular matrix U。

而 **Gauss-Jordan elimination** 更进一步：

  

> 不仅消掉主元下面的元素，还把主元上面的元素也消掉，最后把矩阵化成 diagonal matrix，甚至可以进一步化成 identity matrix。

  

这样做的好处是，变量之间不再相互依赖，求解更直接。

---

## **12. Gauss-Jordan 的代价与优点**

  

PPT 指出：

- 对单个系统来说，Gauss-Jordan 在 **elimination phase（消元阶段）** 更费
    
- 但它有两个优点：
    

  

### **1. 更适合 parallel computing（并行计算）**

  

因为最后变量相互独立。

  

### **2. 对多个相同 coefficient matrix 的系统更快**

  

因为矩阵被化得更彻底。

---

## **13. When the coefficient matrix changes slightly（系数矩阵轻微变化）**

  

之前我们讨论的是：

- A 不变
    
- b 变
    

  

现在 PPT 讨论另一种情况：

- A 也变了
    
- 但只变了一点点
    

  

有些修改后的问题，不必重新完整做 Gaussian elimination 或求 inverse。

尤其当变化是 **rank-1（秩一）** 的时候，可以高效更新。

---

## **14. Outer Product（外积）**

  

给两个列向量 u,v，它们的 **outer product（外积）** 是：

  

uv^T

  

这是一个矩阵。它有重要性质：

- rank \le 1
    
- 如果 u,v\neq 0，则 rank = 1
    
- 任意 rank-1 matrix 都能写成某个 outer product
    

---

## **15. Rank-One Modification（秩一修正）**

  

如果矩阵 B 和 A 的关系是：

  

B=A-uv^T

  

那么我们说 B 是 A 的一个 **rank-one modification（秩一修正）**。

  

如果你已经有 A 的 LU factorization，并且已经解过：

  

Ay=b

  

那么你可以更快求解：

  

Bx=b

  

而不必从头再做完整分解。

---

## **16. Sherman-Morrison Formula（Sherman-Morrison 公式）**

  

PPT 给出：

  

(A-uv^T)^{-1} = A^{-1} + (1-v^TA^{-1}u)^{-1}A^{-1}uv^TA^{-1}

  

这个公式告诉我们：

rank-one modification 的 inverse 可以由原来的 A^{-1} 快速更新得到。

  

并且：

A-uv^T \text{ invertible } \iff 1-v^TA^{-1}u\ne 0

  

这是一条很重要的可逆条件。

---

## **17. 用 Sherman-Morrison 求解新系统**

  

如果：

- Ay=b
    
- Az=u
    

  

那么 Bx=b 的解可写成：

  

x = y+\frac{v^Ty}{1-v^Tz}z

  

这样只要复用已有 LU，就能在 O(n^2) 时间内完成，而不是重新 O(n^3) 分解。

---

## **18. Iterative Refinement（迭代修正）**

  

假设你解 Ax=b 得到了一个近似解 x_0。

定义 **residual（残差）**：

  

r_0=b-Ax_0

  

然后再解一个修正系统：

  

Az_0=r_0

  

再令：

  

x_1=x_0+z_0

  

那么 x_1 会比 x_0 更好。

---

## **19. 为什么这个修正是合理的**

  

因为：

  

Ax_1=A(x_0+z_0)=Ax_0+Az_0=(b-r_0)+r_0=b

  

所以从代数上看，这一步修正把原来的残差补回去了。

PPT 进一步说，随着迭代次数 m 增加，相对误差上界会下降。

---

## **20. Iterative Refinement 的局限**

  

PPT 说它 **not practical（不总是实用）**，但有时有帮助。

  

原因包括：

- 要额外保存 LU factorization
    
- residual 计算中有 subtraction，可能发生 **cancellation（消去）**
    
- 想真正改进精度，通常 residual 要用更高精度算
    

  

但它也有优势：

- 能恢复 badly scaled systems 的精度
    
- 有时能帮助稳定原本不稳定的算法
    

---

## **21. Special Linear Systems（特殊线性系统）**

  

除了 diagonal dominance 和 pivoting 话题，Week 4 进一步聚焦：

- **banded systems**
    
- **sparse systems**
    
- 尤其是 **symmetric positive definite matrices（SPD 矩阵）**
    

  

因为这类系统能节省运算和存储。

---

## **22. Cholesky Factorization（Cholesky 分解）**

  

对于 SPD matrix A，存在一个 lower triangular matrix L，使得：

  

LL^T=A

  

这就是 **Cholesky factorization**。

  

它可以看成一种特别好的 LU：

- L 是下三角
    
- U=L^T
    

  

### **优点**

- 不需要 pivoting
    
- 更省计算
    
- 更省存储
    
- 数值上很稳定（对 SPD 系统）
    

---

## **23. Linear Least Squares (LLS)（线性最小二乘）**

  

这是 Week 4 后半部分的重点应用。

  

给定：

- A 是 m\times n 矩阵，通常 m>n
    
- b 是 m-维向量
    

  

我们找一个向量 x，使得：

  

\|Ax-b\|_2

  

最小。

这就是 **Linear Least Squares（线性最小二乘）** 问题。

  

含义是：

Ax 是模型预测值，b 是观测值；我们希望预测尽量接近观测。

---

## **24. LLS 的具体例子**

  

PPT 用体重、身高预测卧推重量：

- 输入：Weight, Height
    
- 输出：Bench Press
    

  

设线性模型为：

  

f(w,h)=x_1w+x_2h

  

我们希望找到参数 x_1,x_2，让这个模型在给定数据上误差平方和最小。

---

## **25. LLS 的矩阵形式**

  

把所有样本写成矩阵后，目标变成最小化：

  

\|Ax-b\|_2^2

  

也就是最小化误差平方和。

---

## **26. Normal Equations（正规方程）**

  

PPT 对

  

f(x)=\|Ax-b\|_2^2

  

展开并对 x 求 gradient，令其为 0，得到：

  

A^TAx=A^Tb

  

这就是 **normal equations（正规方程）**。

---

## **27. 为什么这和 SPD 有关系**

  

如果：

  

\operatorname{rank}(A)=n

  

那么：

  

A^TA

  

是 **symmetric positive definite（对称正定）**。

因此就可以用 **Cholesky factorization** 来解：

  

A^TAx=A^Tb

  

这就把 least squares 问题转化成了一个 SPD 线性系统。

---

# **Week 3–4 主线总结**

---

## **Week 3 的核心主线**

  

### **1. 线性系统的数学结构**

- linear relationship
    
- matrix representation
    
- system Ax=b
    

  

### **2. 问题是否可解**

- consistent
    
- rank
    
- augmented matrix
    
- well-posedness
    

  

### **3. 问题是否敏感**

- vector norm
    
- matrix norm
    
- condition number
    
- well-conditioned / ill-conditioned
    

  

### **4. 如何稳定地解**

- Gaussian elimination
    
- backward substitution
    
- pivoting
    
- LU factorization
    

---

## **Week 4 的核心主线**

  

### **1. 让 Gaussian elimination 更稳、更强**

- partial pivoting
    
- complete pivoting
    
- scaling
    

  

### **2. 进一步的线性系统工具**

- Gauss-Jordan elimination
    
- rank-one modification
    
- Sherman-Morrison formula
    
- iterative refinement
    

  

### **3. 特殊矩阵的高效解法**

- symmetric positive definite matrix
    
- Cholesky factorization
    

  

### **4. 应用到数据拟合**

- linear least squares
    
- normal equations
    
- A^TA is SPD when rankA=n
    

---

# **你复习时最该记住的关键词**

  

## **Week 3**

- **consistent**
    
- **augmented matrix**
    
- **rank**
    
- **well-posed**
    
- **well-conditioned**
    
- **vector norm / matrix norm**
    
- **condition number**
    
- **Gaussian elimination**
    
- **backward substitution**
    
- **pivoting**
    
- **LU factorization** 
    

  

## **Week 4**

- **partial pivoting**
    
- **complete pivoting**
    
- **scaling**
    
- **Gauss-Jordan elimination**
    
- **outer product**
    
- **rank-one modification**
    
- **Sherman-Morrison formula**
    
- **iterative refinement**
    
- **symmetric positive definite (SPD)**
    
- **Cholesky factorization**
    
- **linear least squares (LLS)**
    
- **normal equations** 
    

---

如果你要的话，我下一条可以继续给你整理成 **Week 3–4 考试背诵版 / cheat sheet 版**，会更短、更适合临考前速看。


下面是 **Week 5** 的中文整理版。

我会严格按照 PPT 的内容顺序来讲，做到**清晰、完整、不遗漏**；同时按你的要求，**专业 / 学术名词全部保留英文标注**。内容依据 Week 5 讲义。

---

# **Week 5：Triangular Least Squares and Householder Transformations**

---

## **1. 本周主题总览**

  

Week 5 主要是在 Week 4 的 **Linear Least Squares (LLS)** 基础上继续深入。

核心主线是：

1. 回顾 **Normal Equation**
    
2. 分析它什么时候 **well-posed**、什么时候 **well-conditioned**
    
3. 说明 **Normal Equations** 的数值缺陷
    
4. 引出更稳健的做法：**QR factorization**
    
5. 重点讲 **Householder transformations**
    
6. 最后说明为什么 **QR via Householder** 是比 **Normal Equations** 更 robust 的方法
    

---

# **Part 1：回顾 Least Squares 的背景**

---

## **2. Last Week（上周回顾）**

  

PPT 一开始先回顾 Week 4：

  

**Linear Least Squares (LLS)** 问题的目标是：

对于一个通常 **overdetermined（超定）** 的线性系统

  

A x = b

  

去找一个“最好的近似解” \hat x，使得 A\hat x 尽量接近 b。

  

这里的关键点是：

- 因为系统通常是 **overdetermined**
    
- 所以“exact solution（精确解）存在且唯一”这个问题本身往往不是 well-posed 的
    
- 我们不再追求精确满足 Ax=b
    
- 而是追求让误差最小
    

  

于是，一个非常自然的标准是最小化 **Euclidean norm（欧几里得范数）**：

  

\|Ax-b\|_2

  

也就是让预测值 Ax 离观测值 b 尽可能近。

---

## **3. 为什么最小化 2-norm 是合理的**

  

PPT 这里顺带提到一个 **Remark**：

  

在 **finite-dimensional normed vector space（有限维赋范向量空间）** 中，不同的 norm 之间是 **equivalent（等价）** 的。

也就是说，对任意两个 norm \|\cdot\|_p 和 \|\cdot\|_q，存在正常数 c,C>0，使得：

  

c\|x\|_p \le \|x\|_q \le C\|x\|_p

  

对所有向量 x 都成立。

  

这意味着：

  

> 在有限维空间里，用 \|Ax-b\|_2 作为误差标准是很合理、很自然的，不会和别的 norm 产生本质冲突。

  

当然，本周后面的算法几乎都围绕 **2-norm** 展开，因为 **orthogonal matrices（正交矩阵）** 会特别自然地保留 2-norm。

---

# **Part 2：Normal Equation**

---

## **4. The Normal Equation（正规方程）**

  

Week 4 已经得到，最小化

  

\|Ax-b\|_2

  

等价于求解对应的 **Normal Equation（正规方程）**：

  

A^T A x = A^T b

  

这就是 Week 5 的出发点。

  

这里：

- A 是 m\times n 矩阵
    
- 通常 m\gg n
    
- 所以 A^TA 是一个 n\times n 的 square matrix
    

  

这样就把原本的 least squares 问题，变成了一个 square linear system。

---

## **5. 什么时候 Normal Equation 是 well-posed**

  

PPT 问：

  

> 什么时候求解

> > A^TAx=A^Tb >

> 是 **well-posed** 的？

  

答案是：当 A^TA 是 **nonsingular（非奇异）** 的时候。

  

因为这是一个 square system，well-posed 的条件就是系数矩阵可逆。

所以要求：

  

\operatorname{rank}(A^TA)=n

  

PPT 还提醒一个重要事实：

  

\operatorname{rank}(A^TA)=\operatorname{rank}(A)

  

对任意实矩阵 A 都成立。

  

因此，Normal Equation well-posed 的条件其实等价于：

  

\operatorname{rank}(A)=n

  

也就是 A 必须 **full column rank（列满秩）**。

这也是 least squares 问题最常见的“解唯一”条件。

---

## **6. Positive Definite（正定）与 Cholesky Factorization**

  

PPT 接着指出：

  

如果 \operatorname{rank}(A)=n，那么

  

A^TA

  

不只是 nonsingular，而且还是 **positive definite（正定）** 的。

于是我们就可以使用：

- **Cholesky factorization**
    

  

也就是写成：

  

A^TA = LL^T

  

其中 L 是 **lower triangular matrix（下三角矩阵）**。

  

于是正规方程就变成：

  

LL^T x = A^Tb

  

然后可以用：

1. **forward substitution**
    
2. **backward substitution**
    

  

来解。

  

所以，从纯数学角度看：

  

> **Normal Equations + Cholesky**

> 是一个完全正确的 least squares 解法。

  

但 Week 5 的重点就是：**它虽然 mathematically correct，却 numerically dangerous。**

---

# **Part 3：Normal Equations 的数值缺陷**

---

## **7. Shortcomings of Normal Equations（正规方程的缺点）**

  

PPT 给出一个例子：

  

A= \begin{bmatrix} 1 & 1\\ \varepsilon & 0\\ 0 & \varepsilon \end{bmatrix}

  

其中 \varepsilon 非常小。

  

那么：

  

A^TA

  

会变成一个非常接近 identity matrix 的东西。

PPT 想表达的核心意思是：

  

> A 中本来存在的小差异，在 A^TA 中会被进一步压缩、弱化。

  

更重要的是，若 A 是 square matrix，则有：

  

\operatorname{cond}(A^TA)=\operatorname{cond}(A)^2

  

也就是说：

  

> **forming** A^TA **会把 condition number 平方化。**

  

这是数值分析里非常经典的一点。

---

## **8. 为什么 squaring the condition number 很危险**

  

如果原来 A 的 **condition number** 已经不算小，那么一平方以后会更大。

而 condition number 越大，问题就越 **ill-conditioned**。

  

所以，即使原本的 least squares 问题还算能接受，

通过 Normal Equation 改写之后，得到的 linear system 也可能变得非常敏感。

  

这就是 PPT 的核心结论：

  

> **The normal equation is likely to give us an ill-conditioned linear system, even if the original problem is well-posed.**

---

# **Part 4：换一个角度看 Least Squares**

---

## **9. A different perspective（不同视角）**

  

PPT 接着提出另一种看法。

  

原问题是：

Ax \approx b

  

Normal Equation 告诉我们：

A^TAx=A^Tb

  

若 A^TA 可逆，那么可直接写出：

  

x=(A^TA)^{-1}A^T b

  

于是定义：

  

A^+=(A^TA)^{-1}A^T

  

这叫 **pseudoinverse（伪逆）**。

  

这样，least squares 解可以直接写成：

  

x=A^+b

  

并且 A^+A=I。

---

## **10. 现在问题变成：如何计算** A^+b

  

PPT 说，问题现在转化为：

  

> 如何 numerically 地计算 A^+b

  

这件事既依赖 A，也依赖 b。

于是接下来课程开始分析这个问题的 **conditioning（条件性）**。

---

# **Part 5：Least Squares 的 Conditioning**

---

## **11. Conditioning（条件性）分析思路**

  

PPT 说，可以像 Week 3 那样，把：

- b 看成输入数据
    
- 乘以 A^+ 看成函数
    

  

于是可以分析“输入扰动如何影响输出解”。

  

类似 Week 3，可以得到一个上界，里面会出现：

  

\frac{\|A^+\|\|b\|}{\|x\|}

  

但这里和单纯解 square system 不同，不能直接把它简化成 \operatorname{cond}(A)。

所以要再做进一步变形。

---

## **12. 让** \operatorname{cond}(A) **出现**

  

PPT 通过推导得到：

  

\frac{\|A^+\|\|b\|}{\|x\|} = \frac{\|A^+A\|\|b\|}{\|Ax\|} \le \operatorname{cond}(A)\frac{\|b\|}{\|Ax\|}

  

这里用的是 Euclidean norm。

  

然后 PPT 把

  

\frac{\|b\|}{\|Ax\|}

  

解释成与某个角度 \theta 有关：

  

\frac{\|b\|}{\|Ax\|} = \frac1{\cos(\theta)}

  

其中 \theta 是向量 Ax 和 b 之间的夹角。

---

## **13. The angle** \theta**（角度** \theta**）**

  

PPT 证明了：

  

Ax \perp (b-Ax)

  

也就是：

- 拟合值 Ax
    
- 残差向量 b-Ax
    

  

是 **orthogonal（正交）** 的。

  

证明来自 normal equation：

  

(Ax)^T(b-Ax)=x^TA^Tb-x^TA^TAx=0

  

因为 x 满足 A^TAx=A^Tb。

---

## **14.** \cos\theta **为什么重要**

  

因为 Ax 与残差正交，所以 b、Ax、b-Ax 构成直角三角形关系。

于是：

  

\cos\theta = \frac{\|Ax\|}{\|b\|}

  

所以：

  

\frac{\|b\|}{\|Ax\|}=\frac1{\cos\theta}

  

这就说明 least squares 问题的 conditioning 不只取决于 \operatorname{cond}(A)，还取决于 Ax 和 b 的夹角。

---

## **15. Least Squares 问题 well-conditioned 的条件**

  

PPT 给出的核心结论是：

  

要让 least squares 问题 **well-conditioned**，需要下面这个量小：

  

\frac{\operatorname{cond}(A)}{\cos\theta}

  

这意味着两个条件都要满足：

  

### **1.** \operatorname{cond}(A) **小**

  

也就是矩阵本身不能太 ill-conditioned。

  

### **2.** \cos\theta \approx 1

  

也就是 Ax 和 b 的方向要接近。

  

如果：

  

\cos\theta \approx 0

  

说明 Ax 和 b 几乎正交，那么问题会非常 ill-conditioned。

  

这点很重要，因为它说明：

  

> least squares 的条件性，不仅与矩阵有关，也和拟合质量几何上“贴不贴近”有关。

---

## **16. More on Conditioning（进一步条件性）**

  

PPT 还继续分析：

如果不是只扰动 b，而是连 A 也扰动，那么解 \hat x 的敏感度会更复杂。

  

最后给出一个上界形式：

  

\operatorname{cond}(A)^2\tan\theta + \operatorname{cond}(A)

  

更准确地说，是：

  

\frac{\|\hat x-x\|}{\|x\|} \lesssim \left(\operatorname{cond}(A)^2\tan\theta+\operatorname{cond}(A)\right) \frac{\|\hat A-A\|}{\|A\|}

  

这说明当 A 本身被扰动时，敏感度甚至会出现 \operatorname{cond}(A)^2 项。

---

## **17. 这段结论的直观理解**

  

这告诉我们：

- 如果 A condition 差，问题本来就敏感
    
- 如果 \theta 大，拟合几何上不理想，问题也更敏感
    
- 两者一起差时，least squares 解会很不稳
    

  

所以仅仅“方程有解”还远远不够，

在 least squares 中，**conditioning** 是核心。

---

# **Part 6：不要用 Normal Equations，改用 QR**

---

## **18. An algorithm（一个新算法的思路）**

  

PPT 接下来提出：

  

不要再通过 A^TAx=A^Tb 来解 least squares。

而是直接对 A 做一种类似 Gaussian elimination 的变换。

  

设 A 是 m\times n 且 m>n，并且 A full rank。

经过变换后，可以得到一个等价系统：

  

\begin{bmatrix} R\\ O \end{bmatrix}x \approx c

  

其中：

- R：n\times n upper triangular block
    
- O：(m-n)\times n 的零块
    

---

## **19. 把系统拆成两部分**

  

这个系统可以拆成：

  

Rx \approx c_1

  

Ox = 0 \approx c_2

  

其中

c= \begin{bmatrix} c_1\\ c_2 \end{bmatrix}

  

这里 c_1 是 n-vector，c_2 是 (m-n)-vector。

---

## **20. 关键问题：这种变换为什么有用？**

  

PPT 说，若这个变换只是普通消元，其实并没有明显简化误差最小化问题。

真正有用的情况是：

  

> 这个变换矩阵 E 是 **orthogonal matrix（正交矩阵）**

  

因为正交矩阵保持 2-norm。

---

## **21. Orthogonal matrices（正交矩阵）**

  

若 Q 是 orthogonal，则：

  

Q^TQ=I

  

这意味着它的列向量构成 **orthonormal basis（标准正交基）**。

更重要的是，它保持 Euclidean norm：

  

\|Qv\|_2=\|v\|_2

  

对所有向量 v 都成立。

---

## **22. 为什么 orthogonal transformation 特别好**

  

如果存在 orthogonal matrix Q，使得：

  

\begin{bmatrix} R\\ O \end{bmatrix} = QA, \qquad c=Qb

  

那么：

  

\|Ax-b\|_2^2 = \left\| \begin{bmatrix} R\\ O \end{bmatrix}x-c \right\|_2^2

  

再展开就变成：

  

\|Rx-c_1\|_2^2 + \|c_2\|_2^2

  

这一下问题就被极大简化了。

---

## **23. 为什么这等价于只要解** Rx=c_1

  

因为 \|c_2\|_2^2 跟 x 无关，是常数。

所以最小化

  

\|Ax-b\|_2^2

  

就等价于最小化

  

\|Rx-c_1\|_2^2

  

但 R 是 upper triangular，而且 full rank，所以系统

  

Rx=c_1

  

本身可解。

一旦取这个解，前半部分误差直接变成 0，于是整体误差达到最小。

---

## **24. 这就是 Triangular Least Squares**

  

PPT 把

  

\begin{bmatrix} R\\ O \end{bmatrix}x=c

  

这种问题称为 **Triangular Least Squares**。

而它本质上就等价于解一个 upper triangular system：

  

Rx=c_1

  

然后用 **backward substitution** 即可。

---

# **Part 7：QR Factorization**

---

## **25. How to obtain** Q **?（如何得到** Q**）**

  

PPT 说，这个过程就是 **QR factorization**。

  

即把矩阵 A 分解成：

  

A=QR

  

其中：

- Q：orthogonal matrix
    
- R：upper triangular matrix
    

---

## **26. QR factorization 的意义**

  

PPT 用了一个很形象的解释：

- Q：提供一组新的、干净的、互相垂直的单位向量，也就是“clean, perpendicular unit vectors”
    
- R：记录原始列向量如何由这些正交基组合出来
    

  

也就是说，**QR factorization** 本质上是一种 **orthogonalization（正交化）**。

---

## **27. 常见的 Orthogonalization Methods（正交化方法）**

  

PPT 提到三类方法：

1. **Givens rotations**
    
2. **Householder transformations**
    
3. **Gram-Schmidt orthogonalization**
    

  

然后说明本周重点讲 **Householder**, 因为它 **more numerically stable（数值上更稳定）**。

---

# **Part 8：Householder Transformations**

---

## **28. Householder transformation 的定义**

  

一个 **Householder transformation** 形如：

  

H_v = I - 2\frac{vv^T}{v^Tv}

  

其中 v 是某个非零向量。

  

如果 v 的维度是 k，那么 H_v 就是一个 k\times k 矩阵。

---

## **29. 几何意义：Reflection（反射）**

  

PPT 说，几何上，H_vx 表示把向量 x 对某个经过原点、并且 **orthogonal to** v 的超平面做 **reflection（反射）**。

  

而 reflection 有一个关键性质：

- 不改变长度
    

  

因此 Householder matrix 是 **orthogonal matrix**。

---

## **30. 为什么 Householder 有用：Annihilating components（消掉分量）**

  

给定一个向量

  

a= \begin{bmatrix} a_1\\ \vdots\\ a_k \end{bmatrix}

  

我们希望找到一个 v，使得：

  

H_va= \begin{bmatrix} \alpha\\ 0\\ \vdots\\ 0 \end{bmatrix}

  

也就是说：

  

> 用 Householder reflection 把向量的下面几个分量全部消成 0。

  

PPT 给出的构造方式是：

  

v=a+\operatorname{sign}(a_1)\|a\|_2 e_1

  

其中 e_1 是第一个 standard basis vector。

---

## **31. Householder 的例子**

  

PPT 例子：

  

a= \begin{bmatrix} 2\\ 1\\ 2 \end{bmatrix}

  

其 2-norm 是 3。

因为 a_1=2>0，所以：

  

v= \begin{bmatrix} 2\\ 1\\ 2 \end{bmatrix} + 3 \begin{bmatrix} 1\\ 0\\ 0 \end{bmatrix} = \begin{bmatrix} 5\\ 1\\ 2 \end{bmatrix}

  

然后构造 H_v，就能得到：

  

H_va= \begin{bmatrix} -3\\ 0\\ 0 \end{bmatrix}

  

也就是下面两个分量都被 annihilate 掉了。

---

# **Part 9：用 Householder 做 QR Factorization**

---

## **32. QR factorization using Householder transformations**

  

PPT 说明：

对矩阵 A 的每一列，依次做这种 “annihilation”，就能把矩阵逐步化成 upper triangular matrix。

  

例如对第一列：

- 构造一个 Householder matrix
    
- 把第一列主元以下全部消成 0
    

  

接着在剩余子矩阵里，对第二列做同样的事。

如此继续，最终就得到 R。

---

## **33. Example：第一步**

  

PPT 的例子：

  

A= \begin{bmatrix} 1 & -4\\ 2 & 3\\ 2 & 2 \end{bmatrix} = [a_1\ a_2]

  

对第一列

  

a_1= \begin{bmatrix} 1\\ 2\\ 2 \end{bmatrix}

  

其 norm 是 3。

于是取：

  

v_1= \begin{bmatrix} 1\\ 2\\ 2 \end{bmatrix} + 3 \begin{bmatrix} 1\\ 0\\ 0 \end{bmatrix} = \begin{bmatrix} 4\\ 2\\ 2 \end{bmatrix}

  

然后有：

  

H_{v_1}a_1= \begin{bmatrix} -3\\ 0\\ 0 \end{bmatrix}

  

同时把这个 H_{v_1} 也作用到第二列 a_2，得到新矩阵。

---

## **34. Example：第二步**

  

经过第一步后得到：

  

H_{v_1}A= \begin{bmatrix} -3 & -2\\ 0 & 4\\ 0 & 3 \end{bmatrix}

  

然后看右下角剩余的子问题，也就是向量

  

\begin{bmatrix} 4\\ 3 \end{bmatrix}

  

其 norm 是 5。

于是构造：

  

v_2= \begin{bmatrix} 4\\ 3 \end{bmatrix} + 5 \begin{bmatrix} 1\\ 0 \end{bmatrix} = \begin{bmatrix} 9\\ 3 \end{bmatrix}

  

再构造对应的 Householder matrix，把 3 消掉，得到：

  

\begin{bmatrix} -3 & -2\\ 0 & -5\\ 0 & 0 \end{bmatrix}

  

这就是 R。

---

## **35. 最终得到的** Q

  

因为每个 Householder matrix 都是 orthogonal，而且 transpose 等于 inverse，所以把这些反射组合起来，就得到 Q。

  

也就是说，Householder 过程本质上构造出了：

  

A=QR

  

其中 Q 是若干 Householder transformations 的乘积。

---

# **Part 10：Triangular Least Squares 的最终求解**

---

## **36. Triangular Least Squares**

  

PPT 说，经过 QR factorization 以后，least squares 系统变成：

  

\begin{bmatrix} R\\ O \end{bmatrix}x=c

  

这叫 **Triangular Least Squares**。

  

而它等价于求解：

  

Rx=c_1

  

因为：

- R 是 upper triangular
    
- 所以可以直接用 **backward substitution** 解出 x
    

  

这就是 QR 方法解 least squares 的核心。

---

## **37. Other than Orthogonalization?（除了正交化之外呢）**

  

PPT 最后提到一种别的方法：

- **Singular Value Decomposition (SVD)**
    

  

但这一周没有展开讲，只是提示它也是解决 least squares / conditioning 问题的重要工具。

---

# **Part 11：Lecture Summary（本周结论）**

---

## **38. 本周最重要的结论**

  

PPT 的总结非常明确：

  

> **Normal Equations are mathematically correct but numerically dangerous because they square the condition number.**

>   

> **QR Factorization via Householder is the robust alternative.**

  

也就是：

  

### **Normal Equations**

- 数学上对
    
- 但数值上不够稳
    
- 因为会把 \operatorname{cond}(A) 变成 \operatorname{cond}(A)^2
    

  

### **QR via Householder**

- 不通过 A^TA
    
- 利用 orthogonal transformation 保持 2-norm
    
- 更 **numerically stable**
    
- 更 **robust**
    

---

# **Part 12：ML relevance（和 Machine Learning 的联系）**

---

## **39. ML relevance**

  

PPT 最后提醒，这周学的 **conditioning** 和 **robustness** 对 **Data Science / Machine Learning** 很有用。

  

尤其是当出现 **collinearity（共线性）** 时，least squares 问题会变得更不稳定。

PPT 列出的相关话题有：

- **PCA**
    
- **Ridge Regression (Tikhonov Regularization)**
    
- **Polynomial Regression & the Vandermonde Matrix**
    

  

本质联系是：

  

> 在 ML 中，设计矩阵如果 condition 很差，直接做 least squares 会不稳；

> 因此 numerically stable 的方法和 regularization 都非常重要。

---

# **Week 5 主线总结**

---

## **40. Week 5 的完整逻辑链**

  

Week 5 的整条逻辑可以概括为：

  

### **第一步：从 Least Squares 出发**

  

我们要解的是：

  

\min_x \|Ax-b\|_2

  

这是一个 **Linear Least Squares (LLS)** 问题。

  

### **第二步：先想到 Normal Equations**

  

它等价于：

  

A^TAx=A^Tb

  

若 A full rank，则 A^TA 是 **positive definite**，可以用 **Cholesky factorization**。

  

### **第三步：发现 Normal Equations 数值上危险**

  

因为：

  

\operatorname{cond}(A^TA)=\operatorname{cond}(A)^2

  

会把问题严重放大。

  

### **第四步：分析 least squares conditioning**

  

其好坏不仅取决于 \operatorname{cond}(A)，还取决于：

  

\cos\theta

  

其中 \theta 是 Ax 和 b 的夹角。

  

### **第五步：改走 QR 路线**

  

把 A 做 **QR factorization**：

  

A=QR

  

其中 Q orthogonal，R upper triangular。

  

### **第六步：用 Householder 来构造 QR**

  

因为 **Householder transformations** 更稳定，可以逐列 annihilate 掉下面的分量。

  

### **第七步：最终把 least squares 化成 triangular system**

  

最后只需要解：

  

Rx=c_1

  

即一个 **Triangular Least Squares** 问题。

---

# **你复习时最该记住的关键词**

  

## **41. 核心术语表**

  

这一周最重要的术语有：

- **Linear Least Squares (LLS)**
    
- **overdetermined system**
    
- **Euclidean norm**
    
- **Normal Equation**
    
- **pseudoinverse**
    
- **conditioning**
    
- **condition number**
    
- **orthogonal**
    
- **orthonormal**
    
- **QR factorization**
    
- **orthogonalization**
    
- **Gram-Schmidt**
    
- **Givens rotations**
    
- **Householder transformations**
    
- **reflection**
    
- **annihilation**
    
- **Triangular Least Squares**
    
- **backward substitution**
    
- **Singular Value Decomposition (SVD)** 
    

---

# **你最必须记住的结论**

  

## **42. 考试/复习最关键的几句**

  

### **1.**

  

**Normal Equations** 是 mathematically correct 的 least squares 方法。

  

### **2.**

  

但它 **numerically dangerous**，因为：

  

\operatorname{cond}(A^TA)=\operatorname{cond}(A)^2

  

  

### **3.**

  

Least squares 的 conditioning 不只取决于 \operatorname{cond}(A)，还取决于 \cos\theta。

  

### **4.**

  

**QR factorization** 通过 orthogonal transformation 保持 2-norm，更稳健。

  

### **5.**

  

**Householder transformations** 是构造 QR 的稳定方法。

  

### **6.**

  

最终 least squares 被转化为一个 upper triangular system：

  

Rx=c_1

  

再用 backward substitution 解。

---

如果你要的话，我下一条可以继续给你整理成 **Week 5 的考试背诵版 / cheat sheet 版**，会更短、更适合期末前快速扫一遍。